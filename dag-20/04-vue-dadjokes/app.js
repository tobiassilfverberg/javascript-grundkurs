/**
 * Vue.js Dad Jokes
 *
 */

// Create our Vue app
const app = Vue.createApp({
	data() {
		return {
		}
	},

	methods: {
	},

	computed: {
	}
})

// Mount our Vue app on the element with id `app` in the DOM
app.mount('#app')
